# Next Steps

- Add automated tests for deterministic replacement behavior.
- Add regression tests for old mapping files that still use plain-text keys.
- Expand supported Kerio field patterns for additional message variants.
- Add optional sample input and expected output fixtures that are safe to publish.
- Add CI checks for README examples and CLI smoke tests.
- Add a lightweight README template-alignment check for `README.md` and `README.ru.md`.
